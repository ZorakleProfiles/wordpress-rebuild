---
import TestimonialCarousel from "./TestimonialCarousel.astro";

type TestimonialFilterType = "brokers" | "franchisors" | null;

interface Props {
  type?: TestimonialFilterType;
}

const { type = null } = Astro.props;
---

<section id="testimonials" class="bg-[#f3f3f3] py-20 md:py-24">
  <div class="section-shell">
    <p class="section-eyebrow">Real Systems. Real Results.</p>
    <h2 class="section-title max-w-5xl">
      Franchise leaders use Zorakle to make
      their biggest decisions.
    </h2>
    <p class="mt-4 text-base text-[#56586a]">Here's what they're saying.</p>

    <TestimonialCarousel type={type} />
  </div>
</section>
