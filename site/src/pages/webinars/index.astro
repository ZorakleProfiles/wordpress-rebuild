---
import BlogList from "../../components/BlogList.astro";
import { getPaginatedPostsByTag } from "../../lib/blog.ts";

const WEBINARS_ARCHIVE_PAGE_SIZE = 10;
const pageData = await getPaginatedPostsByTag("webinars & training", 1, WEBINARS_ARCHIVE_PAGE_SIZE);
---

<BlogList
        title="Webinars | Zorakle"
        description="Webinars from the Zorakle network."
        heading="Webinars"
        emptyStateMessage="No webinar posts were found."
        basePath="/webinars"
        postBasePath=""
        posts={pageData.posts}
        currentPage={pageData.page}
        totalPages={pageData.totalPages}
/>

